{
  "name": "pi-provider-kiro",
  "version": "0.2.0",
  "description": "pi extension for Kiro API (AWS CodeWhisperer/Q) — 17 models across 7 families with OAuth authentication",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mikeyobrien/pi-provider-kiro.git"
  },
  "keywords": [
    "pi",
    "kiro",
    "aws",
    "codewhisperer",
    "ai",
    "provider"
  ],
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build"
  },
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.2",
    "@mariozechner/pi-ai": "^0.53.0",
    "@mariozechner/pi-coding-agent": "^0.53.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
